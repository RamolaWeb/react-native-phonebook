import {
  StyleSheet,
} from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  keyContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    padding: 10,
  },
  valueText: {
    fontSize: 15,
  },
})

export default styles